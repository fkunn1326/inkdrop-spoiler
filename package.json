{
  "name": "spoiler",
  "main": "./lib/index",
  "version": "0.2.0",
  "description": "A spoiler plugin for inkdrop",
  "keywords": [],
  "repository": "https://github.com/fkunn1326/inkdrop-spoiler.git",
  "license": "MIT",
  "engines": {
    "inkdrop": ">=5.6.0 <7"
  },
  "scripts": {
    "build": "rimraf lib && rollup -c --bundleConfigAsCjs",
    "watch": "rollup -c --watch --bundleConfigAsCjs"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.1",
    "rimraf": "^5.0.1",
    "rollup": "^3.29.0"
  },
  "dependencies": {
    "unist-util-visit": "^5.0.0"
  }
}
